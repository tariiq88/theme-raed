<section class="s-block s-block--featured-products container">
    <div class="s-block__title">
        <h2 class="text-2xl font-bold text-secondary">المنتجات الأكثر مبيعاً</h2>
    </div>
    <div class="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-4 md:gap-6">
        {% for product in products.most_sales %}
            <custom-salla-product-card product="{{ product }}"></custom-salla-product-card>
        {% endfor %}
    </div>
</section> 
